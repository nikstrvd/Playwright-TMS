pipeline {
  
  agent any
  
  statges {
    
    stage("build") {
      
      steps {
        sh 'npm install '
        sh 'npm build'
      }
      
      stage("test") {
      
      steps {
      }
        stage("deploy") {
      
      steps {
      }
    }
  }
}
}
}
